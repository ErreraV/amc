"""Server package: contains the application's HTTP servers.

Description:
	This package provides the Flask dashboard, the metrics ingestion server
	and the AMC TCP server implementations used by integration runners.

How to run:
	Use the runner scripts in `src/amc/src/runners/` to start individual
	servers or the integrated stack.
"""

# Server implementations
